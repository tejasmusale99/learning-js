// console.log(2 > 1);

// console.log(2 <= 1);

// When using <, >, <=, >= (comparisons) → JavaScript converts null to 0.
// When using == (loose equality) → JavaScript does NOT convert null to 0.

/*
console.log(null > 0) // 0 > 0 false
console.log(null < 0) // 0 < 0 false 
console.log(null == 0) // null == 0 false (null is not convert to zero)
console.log(null >= 0) //0 == 0 true
*/

// Strict Check ===

console.log("2" == 2)  //true  check value
console.log("2" === 2) //false check data type and value tooo

